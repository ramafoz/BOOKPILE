[Service]
# Send success only after the aggregate operational check passes.
# A failed ping must not reclassify a healthy BOOKPILE check as failed.
ExecStartPost=-/usr/bin/curl -q --config /etc/bookpile/healthchecks-operations.curl
